require "spec_helper"

RSpec.describe SimpleFormBootstrap4 do
  it "has a version number" do
    expect(SimpleFormBootstrap4::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
